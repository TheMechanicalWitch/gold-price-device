devicefirmware is for the device. it's an adafruit huzzah 8622 with wifi.
the display is an adafruit oled 64*128 module.
its thrown together and there are certainly unoptimized and unused parts in the code.
change SEK to another currency code for the corresponding exchange rate. the api for the gold price allows some other commodities as well (as far as i remember)
cJson
Curl
are two libraries needed for parsing and fetcing the api responses respectively.
